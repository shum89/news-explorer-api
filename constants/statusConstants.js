/**
 * status codes
 * @type {{SERVER_ERROR: number}}
 */
const statusCode = {
  SERVER_ERROR: 500,
};
/**
 * error codes
 * @type {{MONGO_ERROR: number}}
 */
const errorCode = {
  MONGO_ERROR: 11000,
};

module.exports = {
  statusCode,
  errorCode,
};
